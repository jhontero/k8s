replicaCount: 1

image:
  repository: jhontero/k8s
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000
  nodePort: 30080

resources:
  requests:
    cpu: 100m
    memory: 64Mi